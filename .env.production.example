# =====================================================================
# .env.production.example — template para produção
# =====================================================================
# Em produção, defina estas variáveis no painel do provedor de hospedagem
# (Vercel, Railway, Fly.io, etc.) ou via secret manager.
# NUNCA commite valores reais — este arquivo documenta apenas as chaves.
# =====================================================================

# --- Frontend ---------------------------------------------------------
NEXT_PUBLIC_API_URL=https://api.seu-dominio.com

# --- Backend / Server-only --------------------------------------------
DATABASE_URL=postgresql://USER:PASS@HOST:PORT/DB
REDIS_URL=redis://USER:PASS@HOST:PORT
